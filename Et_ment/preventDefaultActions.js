function stopDefault ( e ) {
    // prevent the defualt browser action W3C

    if ( e && e.preventDefault ) 
        e.preventDefault();

        // a shortcut for stopping the browser action in IE
       else 
       window.event.returnValue = false; 

       return false;

}



       // prevent button from taking you to a lnk

       const preventButton = (e) => {
        if () {}
      }


// Finishing events 
// callbacks
// some nodejs
// reactjs
